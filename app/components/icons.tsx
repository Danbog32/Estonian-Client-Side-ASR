import { X, Copy, Settings, Play, StopCircle, DeleteIcon } from "lucide-react";

export const Icons = {
  close: X,
  copy: Copy,
  settings: Settings,
  play: Play,
  stop: StopCircle,
  delete: DeleteIcon,
};
